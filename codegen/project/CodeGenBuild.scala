import sbt._
import Keys._
  
object CodeGenBuild extends Build {
  
/*
    lazy val root = Project(id = "codegen",
                            base = file(".")) dependsOn(genutils,datautils)

    lazy val genutils = ProjectRef(file("../genutils"),"genutils")

    lazy val datautils = ProjectRef(file("../datautils"),"datautils")
*/
}
